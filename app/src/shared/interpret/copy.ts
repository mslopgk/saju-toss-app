/**
 * 화면에 그대로 나가는 고정 문구.
 *
 * ⚠ **이 파일에는 시스템 프롬프트가 들어가지 않는다.** UI 진입점(`./ui`)이 여기서만 import 하므로,
 *   프롬프트 전문·모델 라우팅·API 매퍼가 번들에 섞이는 경로를 파일 경계로 끊는다.
 *   (`prompt.ts` 는 반대 방향으로 이 파일을 import 한다.)
 *
 * 근거: 문서10 §8(리포트 IA) · §9(법적 체크리스트) · 문서06 §A-2(혈액형 성격론 반증)
 */

import type { SectionId } from './contracts';

export const SECTION_TITLES: Readonly<Record<SectionId, string>> = {
  saju: '일간(日干)과 여덟 글자',
  sipsin: '십신 배치',
  jiji: '지지에 담긴 기운',
  sinsal: '여덟 글자에 붙은 이름표(신살)',
  zodiac: '별자리',
  mbti: 'MBTI',
  blood: '혈액형(말투)',
  intersection: '네 체계가 공통으로 가리키는 당신',
  conflict: '그런데 딱 하나, 어긋나는 지점',
  strength: '신강신약과 용신',
  luck: '대운의 흐름',
};

/**
 * 앱이 리포트 하단에 고정 노출하는 면책 문구.
 * LLM 이 쓰지 않는다(§9 법적 체크리스트 — 문구가 모델 출력마다 달라지면 안 된다).
 */
export const DISCLAIMERS: readonly string[] = [
  '이 리포트는 동양 명리와 성격유형론을 함께 본 해석이며, 과학적으로 검증된 예측이 아닙니다.',
  '혈액형과 성격의 관계는 과학적 근거가 확인되지 않았습니다. 이 앱에서 혈액형은 문장의 말투만 결정합니다.',
  'MBTI 유형은 사용자가 직접 알려준 값이며, 이 앱은 성격 검사를 제공하지 않습니다.',
  '건강·재산·법률·진로에 관한 결정은 해당 분야 전문가와 상의해 주세요.',
];
