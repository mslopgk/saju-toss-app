node_modules/
dist/
.env
.env.local

# Claude Code 개인 설정 (커밋하지 않음)
.claude/settings.local.json

# 서버 배포용 환경파일 — API 키가 들어간다. 서버에만 두고 저장소에 넣지 않는다.
interpret.env
*.env.local

# 산출물
app/sajuapp.ait
coverage/

# 실패한 생성 에셋 — 격리해 두되 커밋하지 않는다(같은 연꽃 12장, 재생성으로 대체됨)
app/docs/store/branch-failed/

# 스킬 설치물. `npx skills add` 가 만드는 것이고 **앱 코드가 아니다.**
# 전역(~/.claude/skills)에 두면 다른 프로젝트에서도 쓸 수 있고, 저장소에 커밋하면
# 제3자 코드가 앱 이력에 섞인다(한 번 그렇게 들어갔다 걷어냈다).
.agents/
skills-lock.json
