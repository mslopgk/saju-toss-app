node_modules/
dist/
.env
.env.local

# Claude Code 개인 설정 (커밋하지 않음)
.claude/settings.local.json

# 서버 배포용 환경파일 — API 키가 들어간다. 서버에만 두고 저장소에 넣지 않는다.
interpret.env
*.env.local

# 산출물
app/sajuapp.ait
coverage/

# 실패한 생성 에셋 — 격리해 두되 커밋하지 않는다(같은 연꽃 12장, 재생성으로 대체됨)
app/docs/store/branch-failed/
