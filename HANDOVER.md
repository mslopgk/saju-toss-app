# 인수인계 — 사주믹스 (sajumix) 토스 미니앱

작성: 2026-09-08 · 마지막 개발 커밋: 2026-08-22 · 저장소 상태: 깨끗함(작업 중이던 것 없음)

이 문서는 **코드와 커밋만 봐서는 알 수 없는 것**을 적는다. 코드 구조·명령 설명은
`app/AGENTS.md` 와 `app/ARCHITECTURE.md` 에 이미 자세하지만, **그 두 문서는 일부 낡았다**
(§7 문서 신뢰도). 낡은 부분을 여기서 덮어쓴다.

확실하지 않은 것은 **[추정]** 이라고 표시했다.

---

## 1. 무엇인가 / 어디까지 됐나

### 한 줄

생년월일·출생시각·성별·출생지를 받아 **사주 + MBTI + 혈액형 + 별자리**를 한 번에 겹쳐 읽어 주는
앱인토스(토스 미니앱) WebView 앱. 궁합 보기 화면이 딸려 있다.

`appName` 은 **`sajuapp`** 이고 **한 번 등록하면 영구히 바꿀 수 없다.** 운영 도메인
(`https://sajuapp.web.tossmini.com`)과 딥링크(`intoss://sajuapp`)에 각인되어 있다.

### 지금 동작하는 것

| 기능 | 상태 |
|---|---|
| 사주 계산 엔진(`SAJU-ENGINE-1.0.0`) | 동작. 결정론 순수함수. 골든셋 회귀 있음 |
| 양력·음력·음력 윤달 입력 | 동작 (1900~2100, 환산 양력 기준으로 판정) |
| 규칙 기반 해석 리포트 | 동작. **네트워크·LLM 없이도 결과 화면이 채워진다** |
| LLM 해석(Anthropic) 덧칠 | 동작. 서버가 뜨면 문장을 갈아 끼우고, 실패하면 규칙 기반 그대로 |
| 궁합 화면 | 동작. 첫 사람을 다시 묻지 않는다 |
| 토스 프리필(생년월일·성별 자동 채움) | 구현 완료. **실기기 확인만 남음** |
| 약관·개인정보 정적 페이지 | 배포되어 있음 (`https://sajumix.kodekorea.kr/terms.html` 등) |

**설계의 핵심 규율 하나**: 화면·LLM 은 **숫자를 만들지 않는다.** 오행 비율·신강지수 같은 값은
엔진이 확정하고, 렌더러와 프롬프트는 그 값을 옮기기만 한다. 이 규율을 깨면 같은 사람에게
매번 다른 숫자가 나온다.

### 아직 안 되는 것 / 안 한 것

- **실기기 테스트가 0회다.** 데스크톱 Chrome 자동화(`npm run ui-smoke`)로 "레이아웃이 성립하는가"
  까지만 봤다. 토스 앱 안 WebView(안드로이드 Chromium / iOS WKWebView)에서 실제 터치·세이프에어리어·
  폰트 스케일·네트워크는 **아무도 본 적이 없다.** 인수인계 시점의 가장 큰 미검증 구간이다.
- **일반 공개 출시 안 됨.** 테스트 채널까지만 올라가 있다(§4.4). 검수 요청도 넣지 않았다.
- 로그인·결제·광고 연동 없음.
- 계산 정확도 이슈 1건이 열려 있다 — 墓庫 지장간 판정(§8).

### 지금 배포 상태 (2026-08-22 기준)

| | |
|---|---|
| 번들 버전 | `20260822-17` |
| deploymentId | `01a027a1-57fa-7f59-ba05-e78ea89878a0` |
| 검수 상태 | `CREATED` (= 검토 요청 전) |
| 공개 여부 | `deployed: false` — **일반 사용자에게 보이지 않는다** |
| 테스트 링크 | `intoss-private://sajuapp?_deploymentId=01a027a1-57fa-7f59-ba05-e78ea89878a0&host=appsInTossHost` |

올라간 번들 파일 자체는 `_보관/app-sajuapp.ait` 에 그대로 있다.

---

## 2. 파일·폴더 지도

### 루트

| 경로 | 무엇인가 | 왜 거기 있나 |
|---|---|---|
| `app/` | 미니앱 프론트엔드 전부 | 실제로 사용자에게 나가는 것 |
| `server/` | LLM 해석 서버 (독립 Node) | **프롬프트·API 키를 클라이언트에서 떼어 놓기 위해** 별도 프로세스 |
| `legal/` | 약관·개인정보처리방침 정적 HTML | 콘솔 심사에 URL 을 제출해야 한다. `sajumix.kodekorea.kr` 에서 서빙 |
| `docs/` | 리서치·계산명세·설계문서 | 계산 규칙의 **근거**. 건드리기 전에 읽어야 하는 곳 |
| `.claude/` | 이 저장소 전용 스킬 2개 | 앱인토스 정책 점검·초기화 도우미 |
| `_보관/` | 빌드 산출물 4종 | 인수인계 정리 때 옮긴 것. `_보관/README.md` 참조 |

### `app/` 안

| 경로 | 무엇인가 | 알아야 할 것 |
|---|---|---|
| `src/shared/lib/saju/` | **계산 엔진**. 결정론 순수함수 | React·DOM·`Date.now()` 금지. 고치기 전 `docs/research/calc/C00` 필독 |
| `src/shared/lib/compat/` | 궁합 계산 | 배점 근거는 `compat-params.json`(C18 원본) |
| `src/shared/data/` | 빌드타임 데이터 자산 | 절기·음력표·계수. `tools/` 가 굽는 스크립트 |
| `src/shared/knowledge/` | 해석용 지식카드 198장 → `cards.json` | 198장 중 **178장 도달 가능**, 나머지 20장은 불가 사유 확정 |
| `src/shared/interpret/` | 팩트팩 → 카드검색 → 프롬프트 조립 → 응답검증 | **§6.1 의 경계 규칙이 여기 걸려 있다** |
| `src/shared/interpret/ui.ts` | 화면이 쓰는 **유일한** 진입점 | 화면이 배럴(`interpret/index`)을 import 하면 시스템 프롬프트가 사용자 단말로 내려간다 |
| `src/shared/design/` | 자체 디자인 시스템(테마·휠·시트·CTA·필드) | 8월 재설계 때 TDS 를 상당 부분 대체한 결과물 (§7) |
| `src/shared/motion/` | 모션 CSS 규칙 + 카운트업 | `motion.css` 는 `main.tsx` 에서 **한 번만** 싣는다 |
| `src/features/` | onboarding · report · compat | feature 끼리 직접 import 하지 않는다 |
| `src/pages/` | 화면 조립 | `OnboardingPage` · `HomePage` · `DetailPage` · `CompatPage` |
| `docs/store/` | **생성 에셋 원본 PNG (224MB)** | 지우지 말 것 (§6.5) |
| `src/assets/generated/` | 위를 구운 WebP. **앱이 쓰는 것은 이쪽뿐** | `npm run optimize-assets` 산출물이지만 **커밋된다** |
| `scripts/ui-smoke.mjs` | 실제 Chrome 으로 앱을 몰아 보는 검증 | **레이아웃을 보는 유일한 곳** (§6.2) |
| `.env.production` | 빌드 시 주입되는 두 값 | **비밀값 아님**, 그래서 커밋되어 있다 (§4.5) |
| `AGENTS.md` | 개발 규율·함정 모음 (가장 밀도 높은 문서) | 일부 낡음 (§7) |

### `docs/` 안

| 경로 | 무엇인가 |
|---|---|
| `research/calc/C00-계산엔진-통합명세서.md` | **계산의 정본.** 유파 분기 결정표 포함 — 계산을 고치기 전 반드시 읽는다 |
| `research/calc/C01~C23` | 절기·기둥·십신·신강신약·궁합·별자리 각 알고리즘의 1차 근거와 교차검증 |
| `research/calc/proto/` | 검증에 쓴 프로토타입·독립 오라클 | 앱 코드가 아니다. 재검증할 때만 본다 |
| `research/00~15` | 플랫폼·심사·법률·시장 리서치 |
| `research/13-…콘솔-실측…` | 앱인토스 콘솔·심사 제약 (비가역 결정 정리) |
| `superpowers/specs`·`plans` | 8월 재설계의 설계문서와 실행계획 |
| `app/docs/decisions/0001~0005` | 되돌리기 어려운 결정 5건의 기록 |

---

## 3. 준비 (처음 받았을 때)

의존성(`node_modules`)은 들어 있지 않다. **두 군데 각각** 설치해야 한다.

```bash
cd app    && npm install
cd server && npm install
```

Node 20.11 이상. 앱은 `package.json` 기준 Node 22 계열에서 개발했다. [추정 — 정확한 개발 시
Node 패치버전은 기록해 두지 않았다]

---

## 4. 실행·빌드·배포 절차

### 4.1 앱 개발

```bash
cd app
npm run dev        # vite --host 0.0.0.0  ← --host 없으면 실기기에서 접속 못 한다
```

기본 포트는 vite 기본값 **5173**.

### 4.2 검증 — 이 순서를 지킨다

```bash
cd app
npm test           # vitest run
npm run typecheck  # tsc -b --noEmit
npm run lint       # oxlint
npm run build      # tsc -b && vite build && ait build → sajuapp.ait
npm run ui-smoke   # 실제 Chrome 으로 빌드 결과물을 몰아 본다
```

**`ui-smoke` 를 건너뛰지 않는다.** 나머지는 전부 `react-dom/server` 정적 렌더라 **크기·위치를 못 본다.**
그 틈으로 날짜/시각 휠이 통째로 접힌 채 실제 배포까지 나간 적이 있다 — 그때 테스트 1,900개가 전부 초록이었다.

`npm test` 는 `src/` 만이 아니라 **전체**를 돌려야 한다. `vitest run src/` 만 돌리다가
`test/smoke/app-render.test.tsx` 가 화면 이름 변경(ResultPage→DetailPage) 이후 계속 깨져 있는 걸
한동안 아무도 몰랐다.

2026-08-22 마지막 검증 당시 기록: **테스트 2,071건(53파일) 통과 · oxlint 0 · 빌드 정상 ·
ui-smoke 43개 검사 통과 · 초기 청크 gzip 420.67 kB**. (지금은 `node_modules` 가 없어 재확인 불가 —
설치 후 같은 수치가 나오는지가 인수 첫 확인 지점으로 적당하다.)

### 4.3 해석 서버

```bash
cd server
npm install
npm run build                         # esbuild → dist/main.mjs
ANTHROPIC_API_KEY=... npm start       # 키 없으면 기동 자체가 실패한다(의도된 것)
curl localhost:8787/health
```

키가 없을 때 **떠 있는 채로 실패하지 않고 죽는** 이유: 앱이 조용히 규칙 기반 폴백으로 돌아가서
아무도 배포 사고를 눈치채지 못하기 때문이다.

운영 배치: `server/docker-compose.yml` · `server/Dockerfile` · `server/README.md §배포`.
포트 **8787 을 `127.0.0.1` 에만** 바인딩하고 Caddy 가 앞에 선다.

### 4.4 콘솔 업로드 / 출시

```bash
cd app
npm run deploy     # = ait deploy → 테스트 채널(intoss-private)까지만
```

**일반 공개는 여기서 안 된다.** 콘솔에서 '검토 요청' → 검수 3~5영업일 → '출시' 를 눌러야 한다.

> **이 단계는 사람이 누른다.** 전임자와 합의된 규칙이고, 인수 후에도 유지할 것을 권한다.
> 콘솔 MCP 의 `miniapp_update_*` tool 들은 전부 **"수정 + 검토 요청"이 한 몸**이라
> 초안 저장이 없다. 값 하나를 시험 삼아 넣어 볼 수 없으므로 **전부 확정한 뒤 한 번에** 보내야 한다.

콘솔 등록용 이미지는 `app/docs/store/` 에 이미 구워져 있다
(로고 600×600 라이트/다크, 가로 썸네일 1932×828, 세로 스크린샷 636×1048 5장).
재생성은 `npm run gen-store-assets` 와 `npm run ui-smoke -- --shots docs/store`.

### 4.5 환경변수

**앱 (`app/.env.production`, 커밋되어 있음 — 둘 다 비밀값이 아니다):**

| 이름 | 무엇 | 비고 |
|---|---|---|
| `VITE_TOSS_CONSENT_KEY` | 토스 동의 항목 키 | **값이 채워져 있다.** 클라이언트 번들에 실려야 SDK 가 쓴다. 비우면 "토스 정보로 채우기" 버튼이 **에러 없이 사라진다** |
| `VITE_INTERPRET_API_BASE` | 해석 서버 주소 | **값이 채워져 있다**(`https://sajumix.kodekorea.kr`). 비우면 LLM 경로가 통째로 꺼지고 규칙 기반만 돈다 — 그것도 정상 동작 |

동의 항목은 콘솔에서 **`USER_BIRTHDAY` + `USER_GENDER` 둘로만** 구성되어야 한다
(결정 `app/docs/decisions/0004`). 콘솔에서 항목을 늘려도 앱은 그 둘만 읽는다.

**서버:** 견본은 `server/.env.example`. 서버는 `.env` 파일을 읽지 않는다 —
프로세스 매니저/컨테이너의 환경변수 주입만 쓴다. 주요 값:

| 이름 | 기본 | 주의 |
|---|---|---|
| `ANTHROPIC_API_KEY` | **필수** | 없으면 기동 실패 |
| `PORT` / `HOST` | `8787` / `127.0.0.1` | 컨테이너면 `HOST=0.0.0.0` |
| `TRUST_PROXY` | `false` | 프록시 뒤에서만 `true`. **프록시 없이 켜면 레이트리밋이 무력화된다** |
| `INTERPRET_MAX_ATTEMPTS` | `1` | **2 로 올리면 원가가 최대 2배** |
| `MAX_IN_FLIGHT` | `4` | 업스트림 동시 호출 상한 — 원가 방어의 마지막 선 |

### 4.6 자격증명 — 어디에 있나 (값은 여기 적지 않는다)

| 자격증명 | 어디에 |
|---|---|
| `ANTHROPIC_API_KEY` | 운영 서버에만. `~/sajumix-stack/interpret.env` (docker compose `env_file`). systemd 로 돌릴 경우의 위치는 `/etc/sajumix/interpret.env` (0600) — `server/README.md §배포` 참조 |
| 서버 SSH 접속키 | 이 폴더에 **없다**. 인수인계 상위 폴더 `30_계정_API키/SSH키/README.md` |
| 토스 콘솔 접근 | 토스 SSO + 비즈 로그인 계정. 저장소에 자격증명 없음 |
| GitHub 저장소 접근 | `origin` = `https://github.com/mslopgk/saju-toss-app.git` (계정 권한 필요) |

저장소 안에는 API 키가 없다. `legal/.secrets/` 폴더가 있지만 **비어 있다.**

---

## 5. 상대 기관·담당자·약속

| 상대 | 관계 | 확인된 근거 |
|---|---|---|
| **토스 (비바리퍼블리카)** | 입점 심사·배포 플랫폼. 검수 3~5영업일 | 앱인토스 공식 문서 (`docs/research/02`, `13`) |
| **Anthropic** | LLM 해석 공급자. **개인정보 국외 이전 대상**으로 방침에 명시됨 | `legal/privacy.html` |
| **운영 주체** | 사업자등록번호·대표자·연락처(`contact@kodekorea.kr`)가 약관에 기재되어 있다 | `legal/privacy.html §10`, `legal/terms.html` |

**마감일은 문서·커밋 어디에도 없다.** 외부 발주처가 있었다는 근거도 못 찾았다 —
사내 자체 제품으로 보인다. **[추정]**
코드 주석 여러 곳에 "대표님 판단" 으로 기록된 의사결정이 있는 것으로 보아 내부 결정권자가
따로 있었다. **[추정 — 실명은 문서에 없다]**

### 지켜지고 있던 약속 (인수 후에도 유지 권장)

1. **일반 공개 배포 금지.** 지금은 내부 확인용이다. 검토 요청·출시 버튼은 사람이 판단해서 누른다.
2. **`blend` 공유 서버를 건드리지 않는다.** 그 장비에 다른 서비스가 여럿 돈다. 그래서
   시스템 패키지를 얹지 않고 Docker 로만 돌리고, 포트를 루프백에만 열었다.
3. **API 키와 시스템 프롬프트를 클라이언트 번들에 넣지 않는다.**
4. **개인정보처리방침이 실제 동작보다 앞서 있어야 한다.** LLM 경로를 켜기 전에 방침을
   먼저 개정해 뒀다(제2판: 국외 이전 Anthropic/미국, 24시간 메모리 캐시).
   **되돌리면 방침이 거짓이 된다.**

---

## 6. 함정과 주의사항

전임자가 실제로 밟은 것들이다. 대부분 **에러가 나지 않고 조용히 잘못되는** 종류다.

### 6.1 보안 경계 — 이건 회귀하면 사고다

- 화면은 `shared/interpret/ui` 에서만 import 한다. 배럴(`shared/interpret`)을 import 하면
  **시스템 프롬프트가 미니앱 번들에 실려 사용자 단말로 내려간다.**
- 이중 방어: `src/shared/interpret/ui.test.ts`(import 그래프를 정적으로 훑음) +
  빌드 후 `grep '사실 규율' dist/assets/*.js` 결과가 **0** 이어야 한다.

### 6.2 정적 렌더 테스트가 못 보는 것

- SSR 은 `BottomSheet` 본문을 아예 안 그린다. 그래서 휠이 통째로 0px 로 접힌 채
  **테스트 1,900개 전부 초록**인 상태로 배포된 적이 있다.
- **SSR 이 인접한 JSX 표현식 사이에 `<!-- -->` 를 끼워 넣는다.** `{percent}%` 는
  `14<!-- -->%` 가 된다. 문자열 검사가 여기서 두 번 헛돌았다 — 템플릿 리터럴 하나로 합쳐야 한다.
- Emotion 이 CSS 를 SSR 문자열에 주입한다. 전체 HTML 에 정규식(`/\d+%/`)을 걸면
  **스타일 때문에 항상 매치된다.** `<style>` 과 태그를 먼저 걷어내고 검사한다.
- `useEffect` 가 돌지 않으므로 **토스 프리필 자동 호출 자체를 볼 수 없다.**
  그 경로는 `npm run ui-smoke -- --prefill` 이 본다.

### 6.3 CSS·모션에서 두 번 이상 데인 것

- **`position: fixed` 는 조상에 `transform` 이 하나라도 있으면 고정되지 않는다.**
  화면 전환에 translate 를 넣었더니 하단 CTA 가 524px 뷰포트에서 703~757px 로 밀려났고,
  `animation-fill-mode: both` 때문에 그 상태가 **영구히** 남았다.
  → `m-screen` 은 **투명도만** 애니메이션한다. 여기에 transform 을 다시 넣지 말 것.
- keyframes 를 `from{}` 만 쓴다(‘to’ 없음). 끝 상태가 요소의 자연 스타일이 되므로
  **모션이 콘텐츠를 안 보이게 가둘 수 없다.**
- 하단 CTA 는 **불투명 바 + 그 위에 고정 높이 페이드 띠**로 나뉘어 있다.
  바 배경 자체를 그라디언트로 주면 **캡션 줄 수가 바뀔 때마다 높이가 달라져** 캡션이
  반투명 구간에 앉는다. 온보딩(1줄)과 깊이읽기(2줄)에서 각각 재발했다.
- 휠 항목에 `rotateX` 를 쓰지 않는다 — WebView 에서 3D 변환이 글자를 흐리게 만든다.

### 6.4 자체 컴포넌트로 갈아탄 이유 (되돌리기 전에 읽을 것)

TDS(`@toss/tds-mobile`)의 `Wheel`·`BottomSheet`·`FixedBottomCTA`·`ListRow` 를
`src/shared/design/` 의 자체 구현으로 바꿨다. 취향이 아니라 두 가지 이유다.

1. **전부 흰 바탕을 전제한다.** 앱이 어두운 테마로 가면서 흰 띠·흰 스크림이 생겼고,
   그 색은 바깥에서 바꿀 수 있는 값이 아니다. 시트에 자체 휠을 넣자 **흰 바탕에 흰 글자**가 되어
   휠이 통째로 안 보이는 상태로 드러났다.
2. **굴림의 감각이 컴포넌트 내부에 있다.** "생년월일 고르는 게 딱딱하다" 는 지적을
   프롭으로도 CSS 로도 고칠 수 없었다.

`anime.js` 는 **여기서만** 값을 한다(드래그·관성·경계·스냅·스프링, gzip +13.4 kB).
같은 라이브러리를 숫자 카운트업에 쓰는 건 **재 보고 기각했다** — rAF 15줄이 같은 일을 했고
그쪽은 +0.24 kB 다(`shared/motion/useCountUp.ts`).

번들은 재설계 **전후로 오히려 줄었다**(463 → 420.7 kB gzip). TDS 컴포넌트를 덜 쓰게 된 이득이
anime.js 추가분보다 컸다.

`WheelColumn.tsx` 는 지금 **얇은 어댑터**다(자체 `Wheel` 을 감싸기만 한다).
`perspective` 프롭은 호출부를 한꺼번에 고치지 않으려고 남긴 자리라 **아무 일도 하지 않는다.**

### 6.5 에셋

- **원본 PNG(`app/docs/store/`, 224MB)를 지우지 말 것.** Higgsfield 로 만든 것이라
  다시 돌리면 **같은 그림이 나오지 않는다.** 앱이 쓰는 것은 `npm run optimize-assets` 가 구운
  `src/assets/generated/` WebP 뿐이다(71.7MB → 103KB).
- 어두운 배경 상자를 없애는 데 **세 번 실패했다**: `mask-image` 는 남색이 원 안에 남고,
  `mix-blend-mode: screen` 은 어두운 픽셀을 투명하게 만드는 게 아니라 **밝게** 할 뿐이며
  (게다가 `mask` 가 합성 컨텍스트를 분리해 블렌드가 성립하지 않는다),
  sharp 의 `dest-in` 은 입력의 **알파 채널**을 쓰는데 흑백 이미지엔 알파가 없다.
  → 지금은 픽셀마다 밝기를 재서 알파로 굽는다(`THRESHOLD=64`).
  임계를 26 으로 뒀을 땐 배경 밝기(≈36)가 그 위라 **희미한 사각형이 그대로 남았다.**
- 에셋 생성 에이전트를 병렬로 돌릴 때 **스크립트 경로를 공유하지 않는다.** 십이지 첫 판이
  12장 중 9장 똑같은 연꽃으로 나왔는데 원인은 프롬프트가 아니라 경로 충돌이었다.
  산출물은 유효한 PNG 였고 개수도 맞아 **개수 검사로는 안 잡힌다 — 눈으로 봐야 한다.**

### 6.6 LLM 원가 — 구조를 모르고 건드리면 2배가 된다

- 해석은 **분할 호출**이다(요약 1 + 카드 N). 단일 호출은 93.8초였다.
  분할 후 실측: 첫 결과 5.8초 / 전부 58.8초 / 390원 / 4,223자.
- **`cache_control` 을 system 에만 걸면** 팩트팩·지식카드(약 5,800토큰)가 호출마다 전액 과금된다
  (그 상태 실측 **565원**). `toMessagesApiParams` 가 user 턴을 두 블록으로 나눠 접두에도 건다.
  회귀는 `split.test.ts` 가 고정한다.
- **카드를 전부 병렬로 쏘면 안 된다.** 접두 캐시는 사람마다 콜드라, 동시에 쏘면 전부 캐시를
  *쓰고*(입력가 1.25배) 아무도 못 읽는다(실측 **633원**). 첫 장으로 데운 뒤 나머지를 병렬로 보낸다.
- **모델이 지어내는 퍼센트는 프롬프트로 못 막았다.** `15%` 를 금지하니 `20%` 를 썼다.
  금지 대신 **엔진이 맞는 숫자를 준다**(`elementPercent`). 그 뒤 거부 0건.

### 6.7 계산

- 계산을 고치기 전 `docs/research/calc/C00` 의 **유파 분기 결정표**를 먼저 읽는다.
  같은 입력에 다른 답이 나오는 지점이 문서화되어 있고, 임의로 바꾸면 골든셋이 깨진다.
- 음력표를 다시 구우면 **`ENGINE_VERSION` 을 올려야 한다**(캐시 키가 엔진 버전을 포함한다).
- `lunarSource`(음력 원본)는 **표시 전용**이라 캐시 키에 넣지 않는다 — 넣으면 같은 양력일이
  두 키로 갈린다.
- `shared/lib/saju/lunar.ts` 는 데이터 파일 말고 **아무것도 import 하지 않는다.**
  온보딩(초기 청크)이 딥 임포트하기 때문이고, `./pillars` 한 줄이면 간지·납음 상수가 따라 들어온다.

### 6.8 테스트를 쓸 때

문장이 아니라 **구조**를 검사한다. "첫 사람을 다시 묻지 않는다" 를 부제 문구로 확인하던
테스트가 있었는데, 부제를 지우자 **약속은 그대로인데 테스트만 깨졌다.**
그건 화면이 약속을 지킨다는 증거가 아니라 그렇게 적혀 있다는 증거일 뿐이다.
(지금은 "이 화면에 온보딩의 `결과 보기` 경로가 없다" 를 본다.)

새 검사를 넣을 땐 **일부러 버그를 심어 빨간불을 확인하고** 채택했다. 모션 검사 하나는
이 과정에서 두 번 틀린 게 드러났다(기본 `opacity: 0` 을 놓침 / 의도적으로 반투명한 배경을 오탐).

---

## 7. 문서 신뢰도 — 어디가 낡았나

`app/AGENTS.md` 와 `app/ARCHITECTURE.md` 는 **8월 중순 기준**이다. 8/19~8/22 의 UI 대개편이
반영되지 않은 곳이 있다. **코드가 정본이고, 아래는 실제 확인한 차이다.**

| 문서에 적힌 것 | 실제 |
|---|---|
| `ResultPage` | **`DetailPage`** 로 이름이 바뀌었다 |
| `shared/ui` 계층 | 없다. **`shared/design` + `shared/motion`** 이 그 자리다 |
| "MBTI·혈액형은 선택 입력, '모름' 허용" | **둘 다 필수**가 됐고, MBTI 는 16개 목록이 아니라 **네 축 이지선다**(E/I·N/S·T/F·P/J) |
| "휠 높이 240px 을 부모가 줘야 한다 / 지우면 조용히 깨진다" | **해당 함정이 사라졌다.** 자체 휠이 높이를 스스로 갖는다 |
| "앱은 아직 해석 서버 주소를 보지 않는다" | **본다.** `.env.production` 에 들어가 있다 |
| 불변조건 1 "TDS 우선" | 부분적으로만 유효. Provider(`main.tsx`)와 `Spacing`·`Button` 은 남았지만, 표면 컴포넌트는 자체 구현으로 대체됐다 (§6.4) |
| "이 리포트가 참고한 자료" 근거 카드 목록 | **화면에서 제거됐다.** 다만 `usedCardIds` 와 서버의 인용 검증은 그대로다 |
| `app/docs/product.md` 의 흐름도 | 화면 이름·필수 항목이 위와 같은 이유로 낡았다 |

`ARCHITECTURE.md` 의 **번들 분할·TDS 크기 분석**은 여전히 유효하다(초기 청크의 대부분이
`@toss/tds-mobile` 이고, 이 패키지는 단일 모듈이라 **하나만 import 해도 전부 들어온다**).

---

## 8. 다음에 해야 할 일

우선순위 순.

### A. 실기기 테스트 — 다른 모든 것보다 먼저

테스트 링크(§1)를 토스 앱에서 열어 확인한다. 자동화로 **볼 수 없어서** 남은 것들:

1. **휠 스프링 감각.** 헤드리스 합성 터치로는 관성이 실제로 어떻게 느껴지는지 알 수 없다.
   물렁하면 `app/src/shared/design/Wheel.tsx` 의 `createSpring({ stiffness: 120, damping: 18 })`
   가 유일한 손잡이다.
2. **시트 손잡이가 실제로는 안 끌린다.** 모양만 있고 드래그가 붙어 있지 않다.
   실기기에서 끌어 보는 사용자가 있으면 구현하거나 손잡이를 빼야 한다.
3. 종이 질감(`mixBlendMode: overlay`)이 WebView 에서 제대로 합성되는지.
4. 홈 오브젝트가 스크롤 중에도 부드럽게 떠 있는지(무한 애니메이션은 이것 하나뿐이다).
5. AI 요약이 단말에서 6~7초 안에 도착하는지.
6. 세이프에어리어(홈 인디케이터 있는 기기에서 CTA 가 눌리는지).
7. **토스 프리필의 버튼 누른 뒤 흐름.** 동의 → 생년월일 수신 → "이 날짜가 양력이 맞나요?"
   확인 블록의 레이아웃. 분기 로직 자체는 테스트가 전수로 고정하고 있어 **미확인은 레이아웃뿐**이다.

### B. 콘솔 웹에서만 되는 것 두 가지 (MCP tool 이 없다)

1. **사용 연령 19 → 14 로 변경.** 현재 기본값 19~99세인데 운세 앱에 19금은 과하다.
2. **워크스페이스 제휴 약관 동의** — 대표관리자 계정이어야 한다.
   (`releaseDecision: WORKSPACE_TERMS_AGREEMENT_REQUIRED_FROM_OWNER`) **출시를 막는 항목이다.**

### C. 앱인토스 오픈 정책 적합성 점검 — 미완료

`.claude/skills/apps-in-toss-open-policy-checker` 스킬이 있지만 **판정을 내지 못했다.**
그 스킬은 최신 원문을 MCP(`search_docs`/`get_doc`)로 다시 조회하지 않으면 판정을 금지하는데,
해당 MCP 가 인증되지 않아 쓸 수 없었고 공개 정책 URL 은 404 였다.
저장소의 스냅샷(`references/policy-snapshot-2026-07-10.md`)은 참고용이지 판정 근거가 아니다.
→ **콘솔 MCP 를 인증한 뒤(`/mcp` → `apps-in-toss-console` → Toss SSO) 다시 돌려야 한다.**

### D. 키 로테이션 — 검수 후

`ANTHROPIC_API_KEY` 와 토스 관련 키를 검수 완료 후 교체하기로 되어 있었다. 아직 안 했다.

### E. 계산 정확도 — 열려 있는 도메인 결정 1건

**墓庫 지장간 판정.** 판정 방식에 따라 **사용자의 2.476% 에서 신강/신약이 뒤집힌다.**
기술 문제가 아니라 **유파 선택**이라 전임자가 결정을 내리지 않고 남겼다.
근거는 `docs/superpowers/plans/2026-08-19-home-redesign.md` 와 `C00` 유파 분기 결정표.
→ 도메인 판단이 가능한 사람이 정해야 한다.

### F. 서버 실호출 미검증 항목 닫기

`server/README.md` 맨 앞의 경고 표가 아직 열려 있다 **[일부는 8/19 실측으로 닫혔을 수 있다 — 추정]**.
키를 넣고 `npm run smoke` 를 한 번 돌리면 닫힌다: 구조화 출력 스키마 준수, `effort`+`thinking`
조합, 서버측 폴백 베타 개방 여부, 프롬프트 캐시 실제 적중, 토큰 실측.

### G. 여유가 생기면 (급하지 않음)

- **번들 축소**: `cards.json` 198장 중 도달 가능한 **178장만** 굽기
  (`scripts/build-knowledge.mjs`). 단 이건 지연 청크라 **초기 로딩에는 영향이 없다.**
- **해석 캐시를 Redis 로.** 지금은 메모리라 재시작하면 사라지고, 인스턴스가 늘면 히트율이
  대수만큼 나뉜다. `NarrativeCache` 인터페이스(`get`/`set`/`singleFlight`)만 맞추면 된다.
- **낡은 문서 갱신** (§7). `AGENTS.md`·`ARCHITECTURE.md`·`product.md`.

### 일부러 손대지 않은 것 (되돌리지 말라는 뜻은 아님, 이유만 남긴다)

- 궁합 결과 화면 스크롤 5.75배, 깊이읽기 5.7배. 재설계 때 3,516 → 2,990px 까지 줄였고,
  **여기서 더 줄이면 내용을 깎는 쪽**이라 멈췄다. 둘 다 "읽으러 들어온 화면"이다.

---

## 9. 저장소 상태 확인용 사실

| | |
|---|---|
| 브랜치 | `master` |
| HEAD | `0c5a130` — "refactor: 상대방 폼을 온보딩과 같이 줄이고 깊이읽기 카드를 전부 접는다" |
| 커밋 수 | 33 (2026-08-11 ~ 2026-08-22) |
| 원격 | `origin/master` 가 **HEAD 와 같은 커밋을 가리킨다** = 밀어 둔 상태 |
| 작업 트리 | 깨끗함 (미완성 작업 없음) |

**`git status` 가 깨끗하게 보이는 이유를 하나 알아 둘 것.**
인수인계로 더한 `HANDOVER.md` 와 `_보관/` 은 `.git/info/exclude` 에 넣어 뒀다.
추적되는 `.gitignore` 를 고치면 그것 자체가 프로젝트 변경이 되므로, **로컬에만 적용되는** 쪽을 썼다.
프로젝트에 정식으로 남길 생각이면 그 두 줄을 지우고 커밋하면 되고, 폴더째 지워도 코드에는
아무 영향이 없다.

그 밖에는 아무것도 바꾸지 않았다 — `_보관/` 으로 옮긴 넷은 원래 `.gitignore` 대상이라
git 이 애초에 보지 않던 파일들이다. **커밋은 하지 않았고 이력도 건드리지 않았다.**

> 상위 폴더 `INDEX.md` 에 "원격 저장소가 없어 소스 전체 전달이 필요합니다" 라고 적혀 있는데,
> **실제로는 원격이 있고 최신 커밋까지 올라가 있다**(`github.com/mslopgk/saju-toss-app`).
> 다만 그 저장소에 **접근 권한이 살아 있는지는 확인하지 못했다 — 인수 시 확인 필요.**
> 이 폴더의 사본은 원본과 커밋·추적파일 수(469개)가 **정확히 일치**함을 확인했다.
